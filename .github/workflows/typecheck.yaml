on: push

jobs:
   typecheck:
     runs-on: ubuntu/latest
     steps:
       - name: Setup Node.js environment
         uses: actions/setup-node@v2.1.5
         
       - run: yarn --frozen
       - run: tsc .
